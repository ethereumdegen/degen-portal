{
  "name": "x_delete_post",
  "description": "DESTRUCTIVE: delete one of the account's own posts. Returns { data: { deleted: true } }. This is the undo for x_post.",
  "method": "DELETE",
  "url": "https://api.x.com/2/tweets/{id}",
  "headers": {
    "Authorization": "Bearer $X_ACCESS_TOKEN",
    "Accept": "application/json",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Post id, the `data.id` a post returned, e.g. '1790000000000000000'."
      }
    },
    "required": [
      "id"
    ]
  },
  "body_mapping": "none"
}
