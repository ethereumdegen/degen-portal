{
  "name": "x_like",
  "description": "POSTS IN PUBLIC: like a post as the account. Returns { data: { liked: true } }. Likes are visible.",
  "method": "POST",
  "url": "https://api.x.com/2/users/{id}/likes",
  "headers": {
    "Authorization": "Bearer $X_ACCESS_TOKEN",
    "Accept": "application/json",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The acting user's id, from x_me."
      },
      "tweet_id": {
        "type": "string",
        "description": "Post id, the `data.id` a post returned, e.g. '1790000000000000000'."
      }
    },
    "required": [
      "id",
      "tweet_id"
    ]
  },
  "body_mapping": "params_nested"
}
