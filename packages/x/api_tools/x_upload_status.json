{
  "name": "x_upload_status",
  "description": "Where a media upload got to: returns { data: { processing_info: { state, progress_percent } } }. States are pending, in_progress, succeeded, failed. Only needed when x_upload_video reported that X was still transcoding when it gave up waiting.",
  "method": "GET",
  "url": "https://api.x.com/2/media/upload?command=STATUS&media_id={media_id}",
  "headers": {
    "Authorization": "Bearer $X_ACCESS_TOKEN",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "media_id": {
        "type": "string",
        "description": "The id x_upload_video returned."
      }
    },
    "required": [
      "media_id"
    ]
  },
  "body_mapping": "none"
}
