{
  "name": "discord_edit_message",
  "description": "Edit a message the bot sent. Only the bot's own messages can be edited. Returns the updated message.",
  "method": "PATCH",
  "url": "https://discord.com/api/v10/channels/{channel_id}/messages/{message_id}",
  "headers": {
    "Authorization": "Bot $DISCORD_BOT_TOKEN",
    "Accept": "application/json",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "channel_id": {
        "type": "string",
        "description": "Discord channel id, e.g. '1180000000000000000'. Right-click the channel -> Copy Channel ID (Developer Mode on), or use discord_list_channels."
      },
      "message_id": {
        "type": "string",
        "description": "Message id, from the `id` of a message discord_send_message or discord_get_messages returned."
      },
      "content": {
        "type": "string",
        "description": "The replacement text, up to 2000 characters."
      }
    },
    "required": [
      "channel_id",
      "message_id",
      "content"
    ]
  },
  "body_mapping": "params_nested",
  "body_defaults": {
    "allowed_mentions": {
      "parse": []
    }
  }
}
